{
  "name": "mock-api",
  "version": "1.0.0",
  "scripts": {
    "mock-api": "json-server --watch db.json --port ${PORT:-4000} --host 0.0.0.0"
  },
  "dependencies": {
    "json-server": "^0.17.3"
  }
}
